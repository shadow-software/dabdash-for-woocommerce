name: Release

# On a version tag, build the lean installable ZIP and publish a GitHub Release
# with notes — in parallel to the WordPress.org deploy (deploy.yml), and
# independent of it. This is the download a self-hosted store installs directly
# via Plugins → Add New → Upload, without waiting on the wp.org directory or
# needing SVN credentials.
#
# Trigger: push a version tag, e.g.
#   git tag 1.0.0 && git push origin 1.0.0
# The tag MUST equal readme.txt's "Stable tag", the plugin header "Version:", and
# the DABDASH_SYNC_VERSION constant — the build fails loudly if they disagree, so a
# mismatched release can never ship.
on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
      - '[0-9]+.[0-9]+.[0-9]+-*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Existing tag to (re)build a release for'
        required: true

permissions:
  contents: write   # required to create the release and upload the asset

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Build ZIP and publish GitHub Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Resolve the version
        id: version
        run: |
          # The tag drives everything. On a tag push it's github.ref_name; on a
          # manual run it's the supplied input.
          version="${{ github.event.inputs.tag || github.ref_name }}"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "Building release for version: ${version}"

      - name: Verify the tag matches the plugin version everywhere
        run: |
          set -euo pipefail
          tag="${{ steps.version.outputs.version }}"

          header="$(grep -oE '^\s*\*\s*Version:\s*\S+' dabdash-sync-for-wordpress.php | awk '{print $NF}')"
          const="$(grep -oE "DABDASH_SYNC_VERSION',\s*'[^']+'" dabdash-sync-for-wordpress.php | grep -oE "'[0-9][^']*'" | tr -d "'")"
          stable="$(grep -oE '^Stable tag:\s*\S+' readme.txt | awk '{print $NF}')"

          echo "tag=${tag}  header=${header}  const=${const}  stable_tag=${stable}"

          fail=0
          [ "$header" = "$tag" ] || { echo "::error::Plugin header Version ($header) != tag ($tag)"; fail=1; }
          [ "$const"  = "$tag" ] || { echo "::error::DABDASH_SYNC_VERSION ($const) != tag ($tag)"; fail=1; }
          [ "$stable" = "$tag" ] || { echo "::error::readme.txt Stable tag ($stable) != tag ($tag)"; fail=1; }

          if [ "$fail" -ne 0 ]; then
            echo "::error::Version mismatch. Bump every place to ${tag} before tagging."
            exit 1
          fi

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer:v2

      - name: Install runtime Composer dependencies
        env:
          GH_TOKEN: ${{ secrets.SDK_RELEASE_TOKEN }}
        run: |
          if [ -n "$GH_TOKEN" ]; then
            composer config --global github-oauth.github.com "$GH_TOKEN"
          fi
          composer install --no-dev --prefer-dist --no-progress --no-interaction

      - name: Build the lean installable ZIP
        id: build
        run: |
          set -euo pipefail
          slug="dabdash-sync-for-wordpress"
          version="${{ steps.version.outputs.version }}"
          zip="${slug}.${version}.zip"
          staging="${RUNNER_TEMP}/${slug}"

          mkdir -p "$staging" dist
          rsync -a --exclude-from=.distignore ./ "$staging/"
          test -f "$staging/vendor/autoload.php"
          test -f "$staging/dabdash-sync-for-wordpress.php"

          (
            cd "${RUNNER_TEMP}"
            zip -r "${GITHUB_WORKSPACE}/dist/${zip}" "$slug"
          )

          echo "zip=dist/${zip}" >> "$GITHUB_OUTPUT"
          echo "Built dist/${zip}"

      - name: Verify the ZIP is lean and installable
        run: |
          set -euo pipefail
          zip="${{ steps.build.outputs.zip }}"

          echo "── ZIP contents (head) ──"
          unzip -l "$zip" | head -60

          unzip -l "$zip" | grep -q 'dabdash-sync-for-wordpress/dabdash-sync-for-wordpress.php' \
            || { echo "::error::Main plugin file missing from the ZIP"; exit 1; }
          unzip -l "$zip" | grep -q 'dabdash-sync-for-wordpress/vendor/autoload.php' \
            || { echo "::error::vendor/autoload.php missing — SDK not packaged"; exit 1; }

          if unzip -l "$zip" | grep -Eq 'dabdash-sync-for-wordpress/(tests/|docs/|\.github|composer\.(json|lock)|phpcs|phpstan|phpunit|\.distignore|\.gitattributes|\.wordpress-org|README\.md|CONTRIBUTING\.md|SECURITY\.md)'; then
            echo "::error::Development or repo files leaked into the installable ZIP"
            exit 1
          fi

          echo "ZIP is lean and installable."

      - name: Extract release notes from the changelog
        id: notes
        run: |
          set -euo pipefail
          version="${{ steps.version.outputs.version }}"

          body="$(bash .github/extract-changelog.sh "$version" readme.txt || true)"
          if [ -z "$body" ]; then
            body="See the changelog in readme.txt."
          fi

          {
            echo "## DabDash Sync for WordPress ${version}"
            echo
            echo "$body"
            echo
            echo "---"
            echo
            echo "**Install:** download \`dabdash-sync-for-wordpress.${version}.zip\` below, then in your WordPress admin go to **Plugins → Add New → Upload Plugin**, choose the ZIP, and click **Install Now**. Requires a DabDash tenant API base URL and access token."
            echo
            echo "**Also on WordPress.org:** https://wordpress.org/plugins/dabdash-sync-for-wordpress/"
            echo
            echo "**Documentation & source:** https://github.com/shadow-software/dabdash-sync-for-wordpress#readme"
            echo
            echo "Developed & maintained by [Shadow Software LLC](https://shadowsoftware.com/)."
          } > release-notes.md

          echo "Wrote release notes:"
          cat release-notes.md

      - name: Publish the GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: ${{ steps.version.outputs.version }}
          body_path: release-notes.md
          files: ${{ steps.build.outputs.zip }}
          draft: false
          prerelease: ${{ contains(steps.version.outputs.version, '-') }}
          fail_on_unmatched_files: true
